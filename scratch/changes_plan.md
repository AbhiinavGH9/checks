# Changes Plan
1. Remove back chevron button from task modal header (it's hidden by default for desktop anyway - just remove it from HTML)
2. Add subtasks section below notes in task creation step 1
3. Add dirty-check confirm on group & project modal backdrop click
4. Improve subtask threads visually (add connecting line/tree style)
5. Mobile context menu icons - already extracted from DOM via parseMenuDOMToDefinition which reads data-lucide; need to verify destructive styling works in mobile drawer
